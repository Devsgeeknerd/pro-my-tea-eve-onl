import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#f52273",
    },
    secondary: {},
  },
  shape: {},
  components: {},
  MuiTextField: {},
});

export default theme;
